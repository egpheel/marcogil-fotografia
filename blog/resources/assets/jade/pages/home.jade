extends includes/_layout

block navbar
  section.navbar
    hr
    ul
      a(href="#")
        li Categorias
      a(href="#")
        li Crónicas
      a(href="#")
        li Sobre
      a(href="#")
        li Contacto
    hr
    
block content
  section.content
    .featured
      hr
      .img-wrap
        img(src="/img/featured-temp.jpg", alt="Publicação em destaque")
      hr
      .featured-post-wrap
        h2 Pelas ruas de Amesterdão
        p As ruas de Amesterdão são na sua maioria compostas por bicicletas, ou veículos de duas rodas. Mas esta é uma via de carros e tram´s...tive por isso a sorte de apanhar Roger a pedalar pela vida e pela cidade.
        a(href='#').read-more Ler mais
    
    .posts-wrap      
      .posts
        .last-post
          .last-post-img
            img(src='/img/post-temp.jpg', alt='Publicação')
          p.date
            time(datetime!='{{ $posts[0]->created_at->toAtomString() }}') {{ $posts[0]->date }}
          h2 {{ $posts[0]->title }}
          p {{ substr($posts[0]->body, 0, 350) }}{{ strlen($posts[0]->body)>350 ? '...' : '' }}
          ul
            a(href='#')
              li Perspectivas
            a(href='#')
              li Música
            a(href='#')
              li Viver
            a(href='#')
              li Viajar
          hr
          
        | @foreach ($posts as $post)
        | @if ($post->id != $posts[0]->id)
        .post
          .post-img
            img(src='/img/post-temp.jpg', alt='Publicação')
          .post-info
            p.date
              time(datetime!='{{ $post->created_at->toAtomString() }}') {{ $post->date }}
            h2 {{ $post->title }}
            p {{ substr($post->body, 0, 100) }}{{ strlen($post->body)>100 ? '...' : '' }}
            ul
              a(href='#')
                li Perspectivas
              a(href='#')
                li Música
              a(href='#')
                li Viver
              a(href='#')
                li Viajar
        | @endif
        | @endforeach
        {!! $posts->render() !!}
